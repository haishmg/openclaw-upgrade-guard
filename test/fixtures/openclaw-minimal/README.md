# Minimal Public Fixture

This directory intentionally contains no private OpenClaw state.

It exists so the manual GitHub Actions container rehearsal workflow has a safe default fixture path. Maintainers should prefer a reviewed sanitized fixture when investigating real version regressions.
